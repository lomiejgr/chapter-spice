'use client';import {useState} from 'react';import {readers,books} from '@/lib/mock'
export default function Readers(){const[editing,setEditing]=useState(false);return <div className="space-y-6"><h2 className="text-4xl font-black">Lectrices</h2><div className="grid gap-4 md:grid-cols-2">{readers.map(r=>{const read=books.filter(b=>b.status[r.id as 'justine'|'marie']==='Lu').length;return <div key={r.id} className="rounded-[2rem] bg-white p-6 shadow-soft"><div className="grid h-20 w-20 place-items-center rounded-full bg-spice text-3xl font-black text-white">{r.avatar}</div><h3 className="mt-4 text-3xl font-black">{r.name}</h3><p className="font-bold text-spice">{r.role==='Moi'?'Mon profil':'Bibliothèque partagée avec moi'}</p>{r.role==='Moi'&&<button onClick={()=>setEditing(!editing)} className="mt-3 rounded-full border px-4 py-2 font-bold text-spice">Modifier ma fiche</button>}<p className="mt-3 text-stone-600">{editing&&r.role==='Moi'?<textarea className="w-full rounded-2xl border p-3" defaultValue={r.bio}/>:r.bio}</p><p className="mt-4 text-xl font-black">{read} livres lus</p></div>})}</div></div>}
